#include "struct_file.h"
void Creating_folder(WINDOW *);
void Create_file(WINDOW *);
int Write_file(WINDOW *, struct File *, int );
void Read_file(WINDOW *, struct File *, int, char* );
void Clear_subwnd(WINDOW *);
